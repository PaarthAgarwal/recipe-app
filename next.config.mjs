/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "s3-alpha-sig.figma.com",
      "www.themealdb.com",
    ],
  },
};

export default nextConfig;
